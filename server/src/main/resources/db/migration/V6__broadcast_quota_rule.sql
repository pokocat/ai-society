-- =====================================================================
-- 主理人社群运营中台 · V6 群发额度规则化
-- 修正 V5 遗留的护栏 23 违规：派发器"每群每天 1 条"被硬编码在 ContentService。
-- 官方频控（企微文档 92135）：每客户群每月最多接收当月天数条，可在企微管理端
-- 调整为 每周7条 / 每天1条。本列须与企业在企微管理端的群发规则设置保持一致，
-- 默认 1（最严档）；运营调档后在 /rules 同步修改。
-- =====================================================================

ALTER TABLE resource_rules
    ADD COLUMN broadcast_daily_quota INT NOT NULL DEFAULT 1
    CHECK (broadcast_daily_quota >= 1);
